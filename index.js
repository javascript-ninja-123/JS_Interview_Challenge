const {runFunc,run} = require('./execute');
//Arith Geo
//Check Nums
//array addition



const PalindromeCreator = str => {
    str = str.split('');
    const middle = str[Math.ceil(str.length / 2)]
    str.reduce((acc,val) => {

    },)
}

console.log(PalindromeCreator('mmop'))
